<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data >
        <variable
            name="attachment"
            type="com.khtn.zone.model.Attachment" />
    </data>

    <LinearLayout
        android:id="@+id/attachment_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:layout_width="@dimen/dp40"
            android:layout_height="@dimen/dp40"
            app:cardCornerRadius="@dimen/dp20"
            app:cardElevation="0dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="@dimen/dp10"
                android:background="@color/blue_280"
                app:srcAttachment="@{attachment.image}"
                tools:ignore="ContentDescription" />
        </androidx.cardview.widget.CardView>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingVertical="@dimen/dp3"
            style="@style/TextBlackSemi8"
            android:layout_marginBottom="@dimen/dp5"
            android:gravity="center_horizontal"
            android:lines="2"
            android:text="@{attachment.title}"/>
    </LinearLayout>
</layout>